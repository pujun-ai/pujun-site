# Claude Instructions

## Git workflow
- Always create a new branch for every change — never commit directly to `main`
- Branch naming: `claude/<short-description>`
- Open a PR targeting `main` when changes are ready
- One concern per branch — don't stack unrelated changes on the same branch
